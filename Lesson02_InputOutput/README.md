# שיעור 2 — קלט ופלט (תרגול)

תוכנית פתיחה לתרגול קלט ופלט ב-C#.

## איך מריצים
```
dotnet run
```

## המשימות
קובץ `Program.cs` כולל תוכנית בסיסית שמקבלת שם ושנת לידה ומדפיסה גיל, ואחריה הערות `TODO` עם המשימות להוספה:
1. קלט נוסף מסוג `string` (תחביב)
2. קלט נוסף מסוג `double` (גובה בסנטימטרים) והמרה למטרים
3. חישוב מספר הימים שחייתם בקירוב
4. אתגר מסכם — קבלת שני מספרים והדפסת סכום, הפרש, מכפלה ומנה
